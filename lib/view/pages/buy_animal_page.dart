import 'package:flutter/material.dart';

class BuyAnimalPage extends StatelessWidget {
  const BuyAnimalPage({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: SafeArea(
        child: Column(
          children: [
            
          ],
        ),
      ),
    );
  }
}